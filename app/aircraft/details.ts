export type Airport={code:string;name:string;city:string;lat:number;lon:number};
export type AircraftDetails={model:string|null;owner:string|null;flightNumber:string|null;airline:string|null;origin:Airport|null;destination:Airport|null;routeKm:number|null;estimatedMinutes:number|null;service:string;photo:{src:string;link:string;photographer:string}|null;warnings:string[]};
export function greatCircleKm(a:Airport,b:Airport){const rad=Math.PI/180;const dlat=(b.lat-a.lat)*rad,dlon=(b.lon-a.lon)*rad;const s=Math.sin(dlat/2)**2+Math.cos(a.lat*rad)*Math.cos(b.lat*rad)*Math.sin(dlon/2)**2;return 6371*2*Math.asin(Math.sqrt(Math.min(1,s)));}
export function airport(value:unknown):Airport|null{if(!value||typeof value!=='object')return null;const v=value as Record<string,unknown>;if(typeof v.latitude!=='number'||typeof v.longitude!=='number'||!Number.isFinite(v.latitude)||!Number.isFinite(v.longitude)||Math.abs(v.latitude)>90||Math.abs(v.longitude)>180)return null;return {code:String(v.iata_code||v.icao_code||'—'),name:String(v.name||''),city:String(v.municipality||''),lat:v.latitude,lon:v.longitude};}
